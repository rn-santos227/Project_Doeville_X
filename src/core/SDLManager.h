#ifndef SDL_MANAGER_H
#define SDL_MANAGER_H

#include <SDL.h>
#include <string>

namespace Project::Core {
  class SDLManager {
  public:
    SDLManager();
     ~SDLManager();
  };
}

#endif