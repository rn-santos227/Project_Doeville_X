#include "BehaviorSystem.h"

namespace Project::Systems {
  void BehaviorSystem::update(float /*deltaTime*/) {
    PROFILE_SCOPE(Project::Libraries::Constants::BEHAVIOR_PROFILE);
    // Placeholder behavior update job
  }
}
